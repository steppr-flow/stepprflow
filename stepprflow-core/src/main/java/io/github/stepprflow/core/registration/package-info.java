/**
 * Workflow registration client for the monitoring dashboard.
 *
 * <p>This package provides automatic registration of workflow definitions
 * with the StepprFlow monitoring server via the shared message broker,
 * including heartbeat and graceful shutdown support.
 */
package io.github.stepprflow.core.registration;
