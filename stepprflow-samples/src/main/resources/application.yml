spring:
  application:
    name: stepprflow-sample
  profiles:
    active: kafka  # Default to Kafka, override with --spring.profiles.active=rabbitmq

server:
  port: 8010

stepprflow:
  enabled: true

logging:
  level:
    io.github.stepprflow: DEBUG
    io.github.stepprflow.sample: DEBUG